#include "main.h"
/**
 * * _abs - calculates the absolute value of an integer
 * @n: the input value
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
